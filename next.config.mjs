/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      // This option allows you to ignore ESLint errors during builds
      ignoreDuringBuilds: true,
    },
  };
  
  export default nextConfig;
  